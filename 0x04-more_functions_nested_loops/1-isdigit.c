#include "main_h"
#include <stdio.h>

/**
 * _isdigit - a function checking for a digit
 * @c: a character from the function
 *
 * Return: 1 if c is a digit and 0 if not
 */
int _isdigit(int c)
{
	if
	(c >= 0 && c <= 9) {
		return (1);
	else
		return (0);
}
