#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: 0 Always (success)
 */
int mul(int a, int b)
{
	int a, b, result;

	result = a * b;
	return (0);
}

