#include "main.h"

/**
 * _strchr - function that locate a character in a string
 * @s - a string
 * @c - character in the string
 */
char *_strchr(char *s, char c)
{
	char s = [];
	char c;
	return(0)
}
/**
 * @s - a string
 * @d - a character in the string
 * @strlen - lenght of the string
 *
 */
int main(void)
{
	char s = [];
	int d;
	strlen = 9;
	for (d = 0; d < strlen; d++);
		if 
		{
			c = d;
			return(0);
		}
		else
		{
			d != c;
			return(NULL)
		}
	return(0);
}
