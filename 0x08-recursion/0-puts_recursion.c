#include "main.h"
#include <stdio.h>

/*
 * _puts_recursion - writes a string and a trailing newline to stdout.
 * main - check the code
 * @*s - string
 * Return: Always 0.
 */
int main(void)
{
	_puts_recursion("");
	return (0);
}
